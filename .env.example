# Database (required for drizzle MCP)
# Also requires drizzle.config.ts in project root
DATABASE_URL=postgresql://user:password@localhost:5432/quetrex

# Upstash (required for upstash MCP)
# Get from: https://console.upstash.com/account/api
UPSTASH_EMAIL=your-email@example.com
UPSTASH_API_KEY=your-api-key

# GlitchTip Error Monitoring (local dev)
# Start GlitchTip: ./scripts/glitchtip.sh start
# Get DSN from: http://localhost:8000 after creating a project
NEXT_PUBLIC_GLITCHTIP_DSN=http://YOUR_KEY@localhost:8000/1

# Fly.io Deployment (for CI/CD)
FLY_API_TOKEN=your_fly_token
